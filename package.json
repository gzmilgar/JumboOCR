{
  "name": "jumbo-ocr",
  "version": "1.0.0",
  "description": "OCR Service for Jumbo Electronics",
  "main": "srv/server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node srv/server.js",
    "watch": "cds watch",
    "build": "rimraf resources mta_archives && mbt build --mtar archive",
    "watch-ocr-trigger": "cds watch --open com.jumbo.ocr.ocrtrigger/index.html?sap-ui-xx-viewCache=false --livereload false",
    "undeploy": "cf undeploy jumbo-ocr --delete-services --delete-service-keys --delete-service-brokers",
    "deploy": "cf deploy mta_archives/archive.mtar --retries 1"
  },
  "dependencies": {
    "@sap-cloud-sdk/connectivity": "^3.0.0",
    "@sap-cloud-sdk/http-client": "^3.0.0",
    "@sap/cds": "^9.7.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2.2.0",
    "@sap/cds-dk": "^9.2.0",
    "cds-plugin-ui5": "^0.13.0",
    "mbt": "^1.2.29",
    "rimraf": "^5.0.5"
  },
  "cds": {
    "requires": {
      "auth": "dummy"
    }
  },
  "workspaces": [
    "app/*"
  ],
  "sapux": [
    "app/ocr-trigger"
  ]
}
